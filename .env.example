# PostgreSQL Configuration
POSTGRES_USER=raguser
POSTGRES_PASSWORD=CHANGE_ME_IN_DOT_ENV
POSTGRES_DB=rag_platform
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# SQLAlchemy Database URLs (for backend application)
# Async URL for FastAPI application (asyncpg driver)
DATABASE_URL=postgresql+asyncpg://raguser:CHANGE_ME_IN_DOT_ENV@postgres:5432/rag_platform
# Sync URL for Alembic migrations (psycopg2 driver)
DATABASE_SYNC_URL=postgresql://raguser:CHANGE_ME_IN_DOT_ENV@postgres:5432/rag_platform

# Milvus Configuration
MILVUS_HOST=localhost
MILVUS_PORT=19530
MILVUS_USER=
MILVUS_PASSWORD=
MILVUS_COLLECTION_NAME=rag_document_chunks

# LLM Provider Configuration
LLM_PROVIDER=ollama

# Ollama Configuration
OLLAMA_HOST=ollama
OLLAMA_PORT=11434
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_LLM_MODEL=llama3
OLLAMA_EMBED_MODEL=nomic-embed-text
OLLAMA_MODEL_EMBED=nomic-embed-text
OLLAMA_MODEL_LLM=llama3

# OpenAI Configuration (for future)
OPENAI_API_KEY=
OPENAI_LLM_MODEL=gpt-4
OPENAI_EMBED_MODEL=text-embedding-3-small

# Backend Configuration
BACKEND_HOST=backend
BACKEND_PORT=8000
JWT_SECRET=CHANGE_ME_IN_DOT_ENV_GENERATE_RANDOM_STRING

# Frontend Configuration
NEXT_PUBLIC_API_URL=http://localhost:8000

# Logging
LOG_LEVEL=INFO
